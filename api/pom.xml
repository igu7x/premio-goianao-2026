<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>br.jus.tjgo</groupId>
  <artifactId>goianao-api</artifactId>
  <version>1.0.0</version>
  <name>goianao-api</name>
  <description>Premio Goianao - Emissao de Certificados (TJGO)</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <pdfbox.version>3.0.8</pdfbox.version>
    <zxing.version>3.5.3</zxing.version>
    <jjwt.version>0.12.7</jjwt.version>

    <!--
      Alvo padrao do liquibase-maven-plugin: um H2 em arquivo temporario, para
      que `mvn liquibase:updateSQL` gere o SQL sem exigir banco algum nem
      credencial. Para apontar a um ambiente real, sobrescreva na linha de
      comando: -Dliquibase.url=... -Dliquibase.username=... -Dliquibase.password=...
    -->
    <liquibase.url>jdbc:h2:file:./target/liquibase/vazio;MODE=PostgreSQL</liquibase.url>
    <liquibase.username>sa</liquibase.username>
    <liquibase.password></liquibase.password>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!--
      Existe pelas probes do OpenShift: sem um endpoint de saude, o cluster nao
      tem como saber se o pod esta pronto para receber trafego. So o grupo
      health e exposto (ver application.yml).
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!--
      Validacao do id_token do Keycloak. Traz o NimbusJwtDecoder, que busca as
      chaves no JWKS e confere assinatura, emissor e expiracao.

      Nao e a dependencia completa de oauth2-client de proposito: a troca do
      code por token e feita a mao (sao poucas linhas e um RestClient),
      enquanto a parte que NAO se deve escrever a mao, a verificacao
      criptografica, fica com a biblioteca.
    -->
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-oauth2-jose</artifactId>
    </dependency>

    <!--
      Liquibase, e nao Flyway, por dois motivos praticos. E a ferramenta ja
      usada no outro sistema do TJGO que roda no OpenShift, entao a equipe de
      banco a conhece; e o Liquibase OSS gera o SQL de uma migracao pendente
      (liquibase:updateSQL) para revisao previa, no dia em que o DBA pedir para
      aplicar a mao. O Flyway community nao faz isso.
    -->
    <dependency>
      <groupId>org.liquibase</groupId>
      <artifactId>liquibase-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.apache.pdfbox</groupId>
      <artifactId>pdfbox</artifactId>
      <version>${pdfbox.version}</version>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>core</artifactId>
      <version>${zxing.version}</version>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>javase</artifactId>
      <version>${zxing.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <!--
        Existe para o dia em que a equipe de banco pedir para revisar o SQL
        antes de ele rodar: `mvn liquibase:updateSQL` escreve em
        target/liquibase/migracao.sql o que esta pendente, sem tocar no banco.
        A aplicacao continua migrando sozinha na subida; isto e so a via de
        inspecao.
      -->
      <plugin>
        <groupId>org.liquibase</groupId>
        <artifactId>liquibase-maven-plugin</artifactId>
        <configuration>
          <!-- Relativo ao searchPath, que e a raiz dos resources. -->
          <changeLogFile>db/changelog/db.changelog-master.yaml</changeLogFile>
          <searchPath>src/main/resources</searchPath>
          <url>${liquibase.url}</url>
          <username>${liquibase.username}</username>
          <password>${liquibase.password}</password>
          <migrationSqlOutputFile>target/liquibase/migracao.sql</migrationSqlOutputFile>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!--
            Os testes de integracao terminam em *IT e ficam junto do codigo que
            exercitam. Rodam no `mvn test` de proposito: sao a verificacao dos
            criterios de aceitacao das specs, nao uma suite opcional.
          -->
          <includes>
            <include>**/*Test.java</include>
            <include>**/*IT.java</include>
          </includes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
